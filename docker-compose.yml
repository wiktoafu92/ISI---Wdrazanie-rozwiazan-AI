version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: invoices_api
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
      - ./chroma_db:/app/chroma_db
    env_file:
      - .env
    restart: unless-stopped
